version: "3"


services:
  postgres:
    image: postgres:13.3
    environment:
      POSTGRES_DB: "onboarding_bot_db"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
      PGDATA: "/var/lib/postgresql/da ta"
    volumes:
      - ../Init_Database:/docker-entrypoint-initdb.d
      - .:/var/lib/postgresql/data
    ports:
      - "5442:5442"
  web:
    build: .
    container_name: onboarding_bot
    command: python main.py
    ports:
      - 3001:3001
    volumes:
      - ./:/app
    env_file:
      - .envs/.bot
    depends_on:
      - postgres
